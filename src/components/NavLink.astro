---
const { path, title } = Astro.props;

let currentPath = Astro.url.pathname.slice(import.meta.env.BASE_URL.length).replace(/\/$/, '');
---

<a href=`${import.meta.env.BASE_URL}/${path}` class:list={[currentPath ===  path ? 'a-nav-active' : '', Astro.props.class]}>{title}</a>
