---
import config from "../../site/config.json";

const { path, title } = Astro.props;

let currentPath = Astro.url.pathname.slice(config.subdomain.length);

---

<a href=`${config.subdomain}/${path}` class={currentPath === '/' + path ? 'fly-nav-active' : ''}>{title}</a>
