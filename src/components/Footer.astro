---
import { getEntry, render } from 'astro:content';

const footer = await getEntry('templateComponents', 'footer');
const { Content } = await render(footer);
---

<footer class="fly-width-2-3 fly-margin-horizontal-auto">
	<Content />
</footer>
